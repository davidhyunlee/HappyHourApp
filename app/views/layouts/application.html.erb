<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "#happyLA - it's happy hour somewhere!" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%#= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <!-- Header -->
    <div class="header_container">
       <div class="ui grid">
          <div class="four wide column"><%= link_to image_tag("logo.png"), root_path, class: "logo" %></div>
          <div class="eight wide center aligned column">
             <!-- Semantic: Search Field -->
             <!-- Semantic: Search Field End -->
          </div>
          <div class="four wide right aligned column">
             <% if current_user %>
                <div class="login_area">
                <strong><%= current_user.email %></strong>
                <% if current_user.photo? %>
                  <%= link_to image_tag(current_user.photo.url), user_path(current_user), class: "ui avatar image borderit" %>
                <% end %>
                </div>
                <% else %>
                <%= link_to "Log In", '/login' %>
                <%= link_to "Sign in with Facebook", "/auth/facebook" %>
                <%= link_to "Sign Up", '/signup' %>
             <% end %>
          </div>
       </div>
    </div>
    <!-- Header End -->

    <div class="body_container">
       <div class="container">

          <%= yield %>

          <%= params %>  


  <br><br>


    <div class="ui top right pointing dropdown">
      <div class="adf" style="width: 40px; height:40px; background: #000; display: inline-block; border-radius: 3px;">                
        <% if current_user %>
          <% if current_user.photo? %>
            <%= image_tag current_user.photo.url, class: "small_avatar" %>
          <% else %>
            <%= image_tag "unknown_user.png", class: "small_avatar" %>
          <% end %>
        <% end %>
      </div>
      <div class="menu">
        <div class="" style="width: 200px; height: 200px;">sadfasdfas</div>
      </div>
    </div>




       </div> <!-- Container End -->
    </div> <!-- Body Container End -->
  <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
  </body>
</html>
